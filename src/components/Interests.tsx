import Image from "next/image";

const Interests: React.FC = () => {
    return(
        <section id="interests" className="py-20 pt-60">
            <h2 className="text-6xl font-bold mb-4 bg-accent mx-20 py-2 px-2 text-center">Interests</h2>
        </section>
    );
};

export default Interests;